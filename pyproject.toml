[project]
name = "smard-time-series"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "google-cloud-storage>=2.0",
    "python-dotenv>=1.0",
    "requests>=2.31",
    "pandas>=2.0",
    "marimo>=0.13.0",
    "matplotlib>=3.7",
]

[dependency-groups]
dev = []
